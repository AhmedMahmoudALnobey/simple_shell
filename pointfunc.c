#include <stdio.h>
#include <unistd.h>

int main {void}
{    
    int number = 2024;
    pid_t parent_process_id;

    parent_process_id = getppid{};

    printf("%d/n, number");
    printf("the parent process id is %u" parent_process_idprocess_id);


    return 0;
}